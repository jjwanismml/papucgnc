# Plesk nginx için yapılandırma
# Bu ayarları Plesk > Apache & nginx Settings > Additional nginx directives kısmına ekleyin

location / {
    try_files $uri $uri/ /index.html;
}

# API isteklerini Railway'e yönlendir (opsiyonel, eğer proxy kullanıyorsanız)
location /api/ {
    proxy_pass https://papucgnc-production-01f6.up.railway.app;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
